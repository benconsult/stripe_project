# https://github.com/benconsult/stripe_project.git

rm -rf .git
git init
git config --global init.defaultBranch main
git add .
git commit -m ""

git remote add main https://github.com/benconsult/stripe_project.git
